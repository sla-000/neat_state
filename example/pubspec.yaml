name: example
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  built_value: ^8.1.2
  freezed_annotation: ^0.14.3
  neat_state:
    path: ../

dev_dependencies:
  build_runner: ^2.1.2
  built_value_generator: ^8.1.2
  freezed: ^0.14.5